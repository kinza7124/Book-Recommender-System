version: '3.8'

services:
  book-recommender:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    container_name: book-recommender-app
